// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

/* eslint-disable */
/* tslint:disable */
// @ts-nocheck
import {
  AlgebraicType,
  AlgebraicValue,
  BinaryReader,
  BinaryWriter,
  type CallReducerFlags,
  ConnectionId,
  DbConnectionBuilder,
  DbConnectionImpl,
  type DbContext,
  type ErrorContextInterface,
  type Event,
  type EventContextInterface,
  Identity,
  ProductType,
  ProductTypeElement,
  type ReducerEventContextInterface,
  SubscriptionBuilderImpl,
  type SubscriptionEventContextInterface,
  SumType,
  SumTypeVariant,
  TableCache,
  TimeDuration,
  Timestamp,
  deepEqual,
} from "@clockworklabs/spacetimedb-sdk";
import { Quest } from "./quest_type";
import { type EventContext, type Reducer, type RemoteReducers, type RemoteTables } from ".";

/**
 * Table handle for the table `quest`.
 *
 * Obtain a handle from the [`quest`] property on [`RemoteTables`],
 * like `ctx.db.quest`.
 *
 * Users are encouraged not to explicitly reference this type,
 * but to directly chain method calls,
 * like `ctx.db.quest.on_insert(...)`.
 */
export class QuestTableHandle {
  tableCache: TableCache<Quest>;

  constructor(tableCache: TableCache<Quest>) {
    this.tableCache = tableCache;
  }

  count(): number {
    return this.tableCache.count();
  }

  iter(): Iterable<Quest> {
    return this.tableCache.iter();
  }
  /**
   * Access to the `questId` unique index on the table `quest`,
   * which allows point queries on the field of the same name
   * via the [`QuestQuestIdUnique.find`] method.
   *
   * Users are encouraged not to explicitly reference this type,
   * but to directly chain method calls,
   * like `ctx.db.quest.questId().find(...)`.
   *
   * Get a handle on the `questId` unique index on the table `quest`.
   */
  questId = {
    // Find the subscribed row whose `questId` column value is equal to `col_val`,
    // if such a row is present in the client cache.
    find: (col_val: string): Quest | undefined => {
      for (let row of this.tableCache.iter()) {
        if (deepEqual(row.questId, col_val)) {
          return row;
        }
      }
    },
  };

  onInsert = (cb: (ctx: EventContext, row: Quest) => void) => {
    return this.tableCache.onInsert(cb);
  }

  removeOnInsert = (cb: (ctx: EventContext, row: Quest) => void) => {
    return this.tableCache.removeOnInsert(cb);
  }

  onDelete = (cb: (ctx: EventContext, row: Quest) => void) => {
    return this.tableCache.onDelete(cb);
  }

  removeOnDelete = (cb: (ctx: EventContext, row: Quest) => void) => {
    return this.tableCache.removeOnDelete(cb);
  }

  // Updates are only defined for tables with primary keys.
  onUpdate = (cb: (ctx: EventContext, oldRow: Quest, newRow: Quest) => void) => {
    return this.tableCache.onUpdate(cb);
  }

  removeOnUpdate = (cb: (ctx: EventContext, onRow: Quest, newRow: Quest) => void) => {
    return this.tableCache.removeOnUpdate(cb);
  }}
